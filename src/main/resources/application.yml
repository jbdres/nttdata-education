spring:
  application:
    name: education
  datasource:
    url: jdbc:postgresql://localhost:5432/nttdata_education
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect